defmodule FcmMock do
  @moduledoc """
  FcmMock contains:
  - fcm part, which mocks the FCM v1 server responses
  - mock part, which enables error-per-token configuration and stores all FCM activity (requests, responses etc.)
  """
end
